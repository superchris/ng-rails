# This is a controller stub generated from ng-rails
# Inject your own dependencies by adding them to the controller function
# ['$scope', 'MyResource', ($scope, MyResource) ->
# The [] are required to prevent dependencies from being overwritten during
# minification. For more information see 
# http://docs.angularjs.org/tutorial/step_05

<%= application_name.camelize %>.controller '<%= file_name %>Ctrl', ['$scope', ($scope) ->
  # Don't forget to set the scope
  # $scope.entries = Entry.query()
]
